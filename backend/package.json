{
  "name": "ollama-cloud-relay",
  "version": "1.0.0",
  "private": true,
  "description": "Production-ready relay backend for Ollama Cloud App",
  "type": "module",
  "scripts": {
    "start": "node src/index.mjs",
    "dev": "node --watch src/index.mjs",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ioredis": "^5.7.0"
  }
}
